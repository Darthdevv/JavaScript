const array = Array(10) // array size is 10
    .fill()
  .map(() => 10 * Math.random()); // numbers from 0-50 (exclusive)

console.log(array);

const array1 = Array(10) // array size is 10
  .fill()
  .map(() => Math.floor(10 * Math.random())); // numbers from 0-50 (exclusive)

console.log(array1);

const array2 = Array(10) // array size is 10
  .fill()
  .map(() => Math.ceil(10 * Math.random())); // numbers from 0-50 (exclusive)

console.log(array2);

const array0 = Array.of(1, 2, 3, 4, 5, 6, 7, 8, 9);
console.log(array0)


let app = [];
console.log(app)


